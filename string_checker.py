# checks that suers enter a valid response (e.g. yes/ no
# Perimeter / area) based on a list of options
def string_checker(question, num_letters, valid_responses):
    pass


# main routine starts here
yes_no_list = ["yes", "no"]
calculations_list = ["Perimeter", "area"]